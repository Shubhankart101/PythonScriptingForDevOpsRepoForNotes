# Cloud provisioning

## Use case

Use PowerShell to orchestrate Azure CLI and cloud resource operations with preview, validation, and controlled execution.

## Advanced direction

Use managed identity, Azure Resource Graph, parallel inventory, policy remediation, and approval gates.
